# 更新日志

## 未发布

本版把界面进一步对齐原版 GNOME Text Editor：新增分组汉堡主菜单与文档属性侧栏，首选项补上自定义字体开关和清除历史，并彻底修复切换主题时的卡死。

| 范畴 | 更新内容 |
|---|---|
| 版本 1.0.0 | 首个正式版本：版本号从构建日期改为固定 `1.0.0`，AppImage 产物带版本号并统一为 amd64。 |
| 显示名统一 | 中英文应用显示名统一为小写 `vellum`：桌面入口、元数据、窗口标题、标题栏、关于页与菜单同步更新。 |
| 删除内置插件 | 扩展窗口现在允许删除任意插件：用户插件连同磁盘文件删除，内置插件持久化隐藏并立即卸载，重启后不再加载；删除状态保存在用户配置目录。 |
| 新手引导插件 | 新增 `io.github.vellum.welcome` 新手引导插件：首次启动自动展示功能导览，完成后请求自我删除；若选择保留，主菜单会保留“新手引导”入口。 |
| 主题切换卡死 | 上一轮先加“配色未变则跳过”的防御（修复点同一主题卡）；本轮针对真实换色：实测 `gtk_source_buffer_set_style_scheme` 本身接近 0 ms，但 GtkSourceView 会在空闲循环里对整篇文档重算高亮（10 万行约 4 秒 CPU），概览地图还会跟着每帧全量重绘，二者叠加即表现为卡死。现在换色改为异步分片：切换期间先隐藏全部概览地图，当前标签立即应用，其余标签按空闲分片逐个应用，超大后台标签（超过 30 万字符）延迟到被选中时才换色；批次结束恢复地图。主循环在分片之间继续处理输入与绘制，不再整窗冻结。 |
| 主菜单 | 按原版 `primary_menu_model` 结构重做汉堡菜单：新建/打开、保存/另存为/关闭、查找/替换、缩放、文档属性、首选项/快捷键/扩展/关于，分组显示并带快捷键提示（`Ctrl+,` 打开首选项，`Ctrl+Shift+I` 切换文档属性）。 |
| 文档属性侧栏 | 标题栏新增 `info-outline-symbolic` 切换按钮，右侧展开属性面板：文件元信息（名称、位置、类型、编码、换行符；名称/位置可点击用系统应用打开）、缩进配置（自动缩进、制表符/空格、每制表符宽度、每次缩进空格数）与实时统计（行/词/字符）。统计按 400 ms 防抖并在超大文档（超过 50 万字符）跳过全量统计以防卡顿。 |
| 自定义字体 | 首选项新增“使用自定义字体”开关；关闭时回退系统等宽字体，开启后才可用字体选择与缩放。 |
| 清除历史 | 首选项→行为 新增红色“清除历史”危险操作行，确认后删除所有可恢复草稿（当前打开的文档不受影响）。 |
| 缩进空格 | 设置新增 `insert-spaces` 选项，属性侧栏可切换 Tab 键插入真实制表符或空格（默认仍为真实制表符）。 |
| 翻译 | 新增与修正约 50 条中英文翻译，清除全部 fuzzy，修复 4 条历史重复翻译；`msgfmt -c` 全部通过。 |
| 最小窗口尺寸 | 主窗口设置硬性最小尺寸 720×480（顶层窗口 size request 即最小宽度，与原版 `width-request` 一致），低于该宽度布局会错位，禁止继续缩小。 |
| 属性面板错位 | 修复文档属性侧栏“陷进去”的观感：AdwPreferencesPage 默认自带 `margin: 24px 12px` 内缩，放在侧栏里内容四周悬空。现为属性页加 `vellum-properties` 样式，内容贴齐侧栏（`margin: 0`），并铺 `@sidebar_bg_color` 侧栏底色，与编辑器形成清晰分栏。 |
| 菜单缩放修复 | 菜单顶部 −/100%/＋ 按钮此前只弹 toast、实际不改变字号：现在真正读写 `font-scale`（步进 0.1，范围 0.75–2.0），并同步 100% 按钮文本；缩放行布局对齐原版（间距 12、两侧 18 边距、标签 `hexpand` + `pill` 居中）。 |
| 属性面板定宽 | 属性面板宽度从 440 收窄到 400，且不再可拖拽调整：右侧栏由 `GtkPaned` 改为固定宽盒子（编辑器 + 分隔线 + 面板），面板外层套 `GtkScrolledWindow`（内容 400px，外层允许瞬时窄布局收缩），彻底消除窗口变窄时的溢出裁切与 measure 告警；面板内容两侧各留 10px 边距。 |
| 主题切换加固 | 概览地图在换配色时会对整篇文档重绘，超大文档（超过 30 万字符）下会卡死数秒到数十秒。现在这类文档的地图保持隐藏并提示原因，小文档照常恢复；另新增 `VELLUM_DEBUG_TIMING=1` 环境变量，在终端打印主题/配色切换各阶段耗时，便于定位剩余卡死点。 |
| 模态窗口修复 | 主题选择器与字体选择器此前和首选项一样挂在主窗口下（两个 modal 叠加），关闭选择器后首选项失去输入抓取、表现为“冻住”。现选择器改挂在其父首选项窗口下，关闭后正常恢复交互。 |
| 类型选择器 | 属性面板“类型”从内联下拉改为学习原版 GNOME Text Editor 的子菜单方式：行右侧按钮弹出独立子窗口，顶部“Plain Text”加分隔线，下方滚动列出全部 GtkSource 语言，当前项带勾选标记。 |
| 属性面板收窄 | 面板宽度 400 → 360，内容左右边距 10 → 8px，减少“边界太宽”的观感。 |
| 应用图标 | 接入用户提供的 SVG 图标：放至 hicolor `scalable/apps` 主题路径并随包安装；512px PNG 由 2.9MB 旧图换成按 SVG 渲染的 3.8KB 图；开发构建自动向上查找源码 `data/icons/hicolor`，未安装时也能显示图标。 |

## 上一版

本版本修复 AI Completion 在服务返回错误响应时的段错误，把补全行为对齐主流 AI 补全工具，并移除不再需要的 Markdown Mode 扩展。

| 范畴 | 更新内容 |
|---|---|
| AI 段错误修复 | 服务返回 4xx/5xx 且响应体为空时不再解引用空指针；空响应、非 JSON 响应与 `content` 为 null/数组的响应均安全降级为错误提示。 |
| AI 端点归一化 | 裸主机地址（如 `https://api.deepseek.com`）与末尾 `/v1` 基础地址自动补齐 `/chat/completions`；完整 Chat Completions URL 原样使用。 |
| AI 补全体验 | 新增输入停顿约 1 秒后的自动补全（设置中可关闭）；请求同时携带光标前（前缀）与光标后（后缀）上下文，按 fill-in-the-middle 思路组织提示词；继续输入会使在途请求作废并重新调度。 |
| Markdown 移除 | 删除 Markdown Mode 扩展、主菜单入口、快捷键条目与核心 WYSIWYG 标签渲染，相关翻译源与源码 `.vut` 打包同步移除。 |
| 回归测试 | 新增不依赖网络的插件解析回归测试，覆盖空响应、错误体、`content` 各种形态与端点归一化；原端到端 AI 测试补充后缀上下文。 |
| 卡住修复 | 内联补全覆盖层改用 `gtk_text_view_remove` 移除，消除 `gtk_widget_snapshot_child` 断言刷屏导致的界面卡死；偏好设置自动补全开关改连 `notify::active`，消除 `AdwSwitchRow` 上的非法 `state-set` 信号告警。 |
| 独立调试窗口 | F9/F10/F11 的 Build & Run 面板改为独立顶层 GTK 窗口（无 transient/modal 关联），任务栏单独出现，可独立切换、最小化与关闭。 |
| 原版样式学习 | 沙箱无法联网克隆源码，改从本机已安装的 gnome-text-editor 50.1 二进制提取内嵌 GResource（官方 `style.css` 与全部 `.ui` 模板）深度学习；状态栏改为等宽字体、`tnum` 表格数字、`min-width: 100px`、`padding: 3px 12px`、`border-top: @borders`，编辑区四周留白 12px，查找栏改为 slide-up 展开的 `toolbar` 样式，补全幽灵文本去掉斜体、使用等宽字体。 |

## 0.7.1 — 2026-08-26

本修复版处理 AI Completion 请求完成后可能触发的段错误，并将外部文件变更提醒统一为标题栏下方的两行横幅。

| 范畴 | 更新内容 |
|---|---|
| AI 异步生命周期 | 每个请求带有代际令牌。新的请求、扩展停用或会话取消会使旧回调失效；旧回调仍完成自身 I/O 与释放，但绝不访问已停用插件的宿主、窗口或候选控件。 |
| 内联候选清理 | 候选层显式记录所属文档，仅在真实且已挂载的 `GtkTextView` 上创建；切换或关闭标签会清理原文档候选，父控件不一致时安全解绑，避免候选控件在标签关闭后遗留悬空引用。 |
| Markdown 模式 | 自动识别扩展现在读取可保存的原始缓冲区文本，而非被 WYSIWYG 标签隐藏标记后的视觉文本，防止 Markdown Mode 在 220 ms 检测周期中反复启停。 |
| 极简核心模式 | 新增“首选项 → 行为 → 启用扩展”总开关。关闭并重启后不创建插件管理器或加载任何动态模块，窗口仅保留核心编辑、标签、文件、查找、状态栏和首选项能力。 |
| TUI 调试器 | 新增安装目标 `vellum-tui-debug`。它以白名单行协议驱动真实 GTK 窗口、输出 JSON 状态、默认隔离配置且不加载扩展；可测试文本、保存、缩放和界面面板状态。 |
| 回归测试 | 本地 OpenAI 兼容 HTTP 测试覆盖正常候选、Tab 接受和停用后延迟回调；新增真实 `GtkTextView` 覆盖层切换/清理回归及 TUI 异步保存回归。另以一次短请求验证了 `deepseek-v4-flash` 的 Chat Completions 兼容性与非思考模式。 |
| 文件变更横幅 | 提示条固定在标题栏下方，使用标题/说明两行层级、金色深色警示背景、右侧“丢弃更改并重新载入”主操作和关闭按钮；不会自动覆盖本地编辑。 |

## 0.7.0 — 2026-08-26

本功能版本把近期用户反馈转化为更可控的编辑器工作流：快捷键可编辑，AI 补全不再直接写入正文，文件冲突不会静默覆盖本地内容，构建输出也不会再挤占编辑区域。

| 范畴 | 更新内容 |
|---|---|
| 生命周期 | 为主窗口销毁、标签页选择、延迟语言/快照回调、状态标签和插件面板增加存活性检查；关闭窗口后不再访问失效的 `AdwTabView` 或 `GtkLabel`。 |
| 快捷键与缩放 | “键盘快捷键”窗口的每一项现在可捕获新的组合键、立即重绑并保存到 XDG INI；核心动作和已加载扩展均读取该映射。新增 `Ctrl+0` 缩放重置和可持久化的缩放设置。 |
| Markdown 与 AI | Markdown Mode 会按 Markdown 文件后缀或常见结构自动在当前编辑器中启用本地、可编辑的标签式 WYSIWYG 渲染。AI Completion 收到结果后以 GtkTextView 覆盖层显示约 50% 透明的内联候选；`Tab` 接受，`Escape` 取消，候选不会进入缓冲区或保存文件，光标上下文变化即失效。 |
| 外部文件冲突 | 对已打开的磁盘文件添加 `GFileMonitor`。检测到外部改动后显示非模态提示条，用户可关闭提示或选择“丢弃更改并重新载入”；不会自动覆盖当前编辑内容。 |
| Vi 模式 | 扩展对外更名为 **Vi Mode / Vi 模式**，继续保留 Vim 常用普通/插入按键。普通模式按 `:` 可打开命令行，支持 `:w`、`:wq`、`:x`、`:q`、`:q!` 和 `:help`。 |
| Build & Run | F9/F10/F11 的构建、运行和构建后运行输出迁移到独立非模态工具窗口，保留构建/运行/停止/清空控制以及不经 shell 的直接参数解析。 |
| 图标 | 新增原创 Vellum 纸页、墨线与笔尖图标，作为桌面启动器、About 窗口和 AppImage 的品牌标识；不使用或改绘其他编辑器图标资源。 |

## 0.6.2 — 2026-08-26

本修复版处理保存文档时可能让界面停留在等待状态的问题，并将保存路径覆盖到窗口动作级回归测试。

| 范畴 | 更新内容 |
|---|---|
| 异步保存 | 不再通过 GtkSourceFileSaver 保存文件；现在从主线程安全快照当前文本后，使用标准 GIO `g_file_replace_contents_async()` 在后台执行原子替换写入。完成或错误均回到主循环处理，不同步等待文件系统。 |
| 保存状态 | 文档增加“保存进行中”保护。重复 `Ctrl+S` 或多个另存为对话框竞态不会再次启动写入；界面会显示“正在保存…”或“保存正在进行中”。 |
| 数据一致性 | 仅在写入成功后提交目标文件路径、清除修改状态并删除恢复快照；失败时保留未保存状态与错误提示。GIO 保存会保留用户缓冲区的原始文本，不再额外改写末尾换行。 |
| 回归测试 | 新增直接文档保存测试和实际窗口保存动作测试；二者在 Xvfb 与 `G_DEBUG=fatal-warnings` 下验证回调完成、内容落盘与修改状态复位。 |

## 0.6.1 — 2026-08-26

本修复版解决扩展元数据显示中的 Adwaita markup 解析问题，并将代码主题从普通下拉框升级为独立的可视化选择工作流。

| 范畴 | 更新内容 |
|---|---|
| 主题选择器 | “首选项 → 代码主题”现在打开两列主题卡片窗口。每张卡片以 GtkSourceView 实际 C 语法高亮渲染预览；点击即应用并持久化主题。卡片使用系统已安装的方案，不包含或复制第三方主题资源。 |
| Build & Run 修复 | 扩展名称和说明在写入 Adwaita 偏好行前会进行 Pango markup 转义，`Build & Run` 中的 `&` 不再触发 GTK/Adwaita Critical；设置页中的保存操作同样已修复。 |
| 扩展中心 | 启用开关保留标准控件；导出、配置和删除改为带工具提示的紧凑图标操作，移除拥挤的宽文字按钮。 |
| 本地化 | 主题选择器及紧凑扩展操作提示已加入 English 和简体中文语言包；同时纳入标准 gettext 源文件清单。 |

> Vellum 的主题选择工作流为独立实现；仅在运行时向 GtkSourceView 查询本机安装的配色方案，未复制 GNOME Text Editor 或其他 GPL 项目的界面源码、样式表或主题资源。

## 0.6.0 — 2026-08-26

本版本将 Vellum 的可分发扩展统一为可审阅、可本机构建的源码 `.vut` 包，并补齐多文件与代码编辑工作流所需的可选功能和通用首选项。

| 范畴 | 更新内容 |
|---|---|
| `.vut` 扩展包 | 新增带 ZIP 清单、CRC 校验、OS/架构/ABI/插件 API 校验的 `.vut` 格式；二进制包必须精确匹配目标，源码包可在同一 OS 使用 `architecture=any` 与 `abi=any` 按本机重新构建。 |
| 源码分发 | Timestamp、Document Statistics、AI Completion、Test Links、Project Sidebar、Build & Run、Markdown Preview 和 Vim Mode 均生成源码 `.vut` 包。每个包包含独立源码、插件 ABI 头文件、Makefile 和依赖声明；导入源码包需要本机的 `make`、`cc`、`pkg-config` 及相应开发依赖。 |
| 扩展中心 | 扩展页面新增持久化启用开关。禁用会停用扩展、移除它的面板、动作和快捷键；重新启用只激活该模块。原生扩展仍必须来自可信来源。 |
| 新扩展 | 新增 Project Sidebar、Build & Run、Markdown Preview 与 Vim Mode。构建/运行只执行用户在扩展设置中配置的直接参数命令；Markdown 只做本地文本渲染；Vim Mode 提供普通/插入模式及基础移动、删除和撤销重做。 |
| 编辑首选项 | 新增行号、当前行高亮、GtkSourceMap 概览图、右边界、自动折行、自动缩进、缩进列宽与会话恢复开关；Tab 继续固定插入真实 U+0009。 |
| 快捷键 | 新增原生“键盘快捷键”窗口，列出 Vellum 已实现的文档、编辑和扩展动作。 |
| 验证 | `.vut` 单元测试覆盖 ZIP 导入导出、CRC 和源码构建；全部八个内置源码包在隔离目录按其清单成功生成模块；文档行为测试和 Xvfb 生命周期冒烟测试通过。 |

> 源码 `.vut` 是受控的本机构建流程，不是沙箱。构建与加载的原生代码以当前用户权限运行；仅导入可信来源的扩展包。

## 0.5.0 — 2026-08-26

本版本完成编辑体验增强、插件验证和许可证来源审计。新增实现均由 Vellum 项目独立编写；未向发行树引入 GNOME Text Editor 或其他 GPL 项目的源码、样式表或资源。

| 范畴 | 更新内容 |
|---|---|
| 真实 Tab | 编辑器现在固定写入 U+0009 Tab 字符；新增 1–16 列的显示宽度偏好，该设置不再把 Tab 展开为若干空格。 |
| 运行时语言识别 | 打开文件后和停止编辑约 450 ms 后，按文件名、shebang 与常见代码/结构特征重新推断语法语言；未命名缓冲区同样可识别 JSON、Python、C/C++、Rust、Go、Java、SQL 等。 |
| 轻量诊断 | 使用 GTK 原生文本标签和 Pango 错误下划线标记错配/未闭合括号及未闭合引号。这是非阻塞的启发式编辑提示，不替代编译器或语言服务器。 |
| 链接测试 | 新增 `Test Links` 原生扩展；按 `Ctrl+Shift+L` 后只对当前文档中最多 32 个唯一 HTTP/HTTPS 链接执行 Range GET，并汇总可达与失败数量。 |
| AI 补全 | 兼容设置中已经保存的 `/v1` 基础地址，在请求时自动补足 `/chat/completions`；保留 `stream: false`、响应格式兼容与详细 HTTP 错误提示。 |
| 验证 | 以本地回环服务验证 AI `/v1` 请求和链接测试；以 GTK 测试验证 Tab、内容语言识别与诊断标签；在 Xvfb 下加载四个内置扩展并进行 7 秒生命周期冒烟测试。 |
| 许可证 | 项目自有源码、文档和元数据确认采用 BSD-2-Clause；`LICENSE_AUDIT.md` 记录来源审计、移除 GPL 参考树与动态运行时依赖边界。 |

> 原生插件在编辑器进程内以当前用户权限执行。AI 与链接测试功能都必须由用户手动触发；请仅使用可信扩展和服务地址。

## 0.3.0 — 2026-08-26

本版本重新设计暗色代码编辑体验，并将扩展中心从只读列表升级为可操作与可配置的模块管理器。

| 范畴 | 更新内容 |
|---|---|
| 暗色主题 | 移除固定的紫黑编辑区、光标、选区与工具栏颜色覆盖；窗口控件回归 Libadwaita 默认表面，编辑区和 token 色完整使用 GtkSourceView `Adwaita-dark`。实现借鉴 GNOME Text Editor 的分层原则，但未复制其 GPLv3 源码、CSS 或资源。 |
| 扩展中心 | “扩展”页面支持导入原生模块至用户扩展目录、将已加载模块导出为副本、删除用户导入模块，以及按模块提供的可选配置入口。系统捆绑模块不会显示删除操作。 |
| 插件 ABI | 新增 `get_text_before_cursor` 服务和可选 `mt_plugin_configure` 导出，使扩展可申请受控的编辑上下文并提供自身设置页面。 |
| AI 补全 | 新增 `AI Completion` 模块。用户输入 OpenAI 兼容 URL、模型和 API 密钥后，可用 `Ctrl+Shift+Space` 手动请求补全。请求异步执行，发送光标前最多 8,000 字符，返回内容在光标处插入。 |
| 隐私 | AI 密钥保存至用户配置目录、设置为 `0600`；没有配置或没有手动触发时不会发生网络请求。 |
| 测试 | 使用回环地址的模拟 OpenAI 兼容服务，通过插件 ABI 验证 JSON 响应解析和补全文本插入；没有使用真实服务或密钥。 |

> 导入的原生扩展与 Vellum 在同一进程执行。请只导入来源可信、与当前 ABI 匹配的 C 共享模块。

## 0.2.0 — 2026-08-26

本版本将 Vellum 从基础编辑器扩展为具有完善本地化、代码体验和可见扩展管理能力的 GTK4 应用。

| 范畴 | 更新内容 |
|---|---|
| 本地化 | 新增独立 `en.po` 英语包，保留并补全 `zh_CN.po` 简体中文包；首选项支持系统语言、English 与简体中文。 |
| 字体 | 首选项接入 GTK4 `GtkFontDialog`，可选择系统安装的编辑器字体族并持久化；保留字体缩放。 |
| 深色主题 | 编辑区、光标、选区、状态栏与标签区域采用优化的深色颜色；跟随系统时会监听系统主题变化。 |
| 代码颜色 | 所有文档使用 GtkSourceView 的 `Adwaita` / `Adwaita-dark` 风格方案，按语言类别着色。 |
| 自动识别 | 在扩展名外分析 shebang、JSON、XML、HTML 和 YAML 特征，并在状态栏显示识别语言。 |
| 扩展系统 | 新增“扩展”管理面板，可显示模块名称、描述和版本；插件 ABI 新增 `get_current_text` 服务。 |
| 示例扩展 | 新增 `Document Statistics`，通过 `Ctrl+Shift+W` 显示当前文档字符、词语和行数；保留 `Timestamp` 扩展。 |

> 原生扩展与应用在同一进程运行。请仅将可信的 C 共享模块置于应用或用户扩展目录中。

## 0.1.0 — 2026-08-26

首次交付：多标签编辑、GtkSourceView、查找替换、草稿恢复、基础主题设置、中英界面、动态插件 ABI、Linux AppImage 测试包与 Windows MinGW 交叉编译配置。
